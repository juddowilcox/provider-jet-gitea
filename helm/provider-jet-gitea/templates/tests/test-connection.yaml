apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "provider-jet-gitea.fullname" . }}-test-connection"
  labels:
    {{- include "provider-jet-gitea.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "provider-jet-gitea.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
